# 2026-04-28 - TS Picture Agent Setup

## Summary
Completed the bootstrap and initial setup for the Hamburg Overhead Door picture processing agent.

## Database Connections
- **Database:** MS SQL Server at `hod-edge` (`192.168.1.15`).
- **Credentials:** Username `ts`, Password provided by William.
- **Data Source:** `TicketPic` table, `PicData` column (Base64 encoded JPEGs).

## Network Storage
- **Destination:** `\\hod-edge\SHARED\TicketPics` (mapped locally as `~/ticketpics_drive` during testing).
- **Credentials:** Domain `HOD`, Username `william`, Password provided by William.

## Automated Sync
- **Logic:** Extracts pictures from the last 30 days, decodes Base64 to JPG, and saves them into folders named after the `TicketID`.
- **Schedule:**
  - Mon-Fri: 8 AM - 5 PM (Hourly)
  - Sat: 9 AM - 12 PM (Hourly)
- **Deployment:** Managed via system crontab calling `sync-cron.js`. Logs are stored in `sync.log`.

## Files Created
- `sync-cron.js`: The production sync script.
- `sync-batch.js` / `sync-refined.js`: Evolution of the sync logic.
- `IDENTITY.md`, `USER.md`, `SOUL.md`: Identity and mission documentation.
